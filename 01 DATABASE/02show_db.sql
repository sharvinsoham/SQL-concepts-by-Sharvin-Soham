SHOW DATABASES -- Get a list of all databases in Mysql

-- MS SQL Server doesn't have a specific command to show all databases as a result we run query to show all databases.
SELECT * FROM sys.databases
GO;